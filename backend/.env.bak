# Postgres connection (inside Docker)
DB_HOST=db
DB_PORT=5432
DB_NAME=pocketsaver
DB_USER=pocketsaver
DB_PASS=hunter2

# Express server port (container) - FIXED to match Dockerfile
PORT=5000

# JWT secret
JWT_SECRET=Shweta123

# CORS allow from your frontend on host
CORS_ORIGIN=http://localhost:3001

DATABASE_URL=postgres://pocketsaver:hunter2@db:5432/pocketsaver
FORECAST_URL=http://forecast_service:8080/predict   # if you deploy the python service
